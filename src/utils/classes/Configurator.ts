// import { program } from 'commander'
export interface ConfiguratorOptions {
  reformat: string
}

export class Configurator {}

if (require.main === module) {
  // program.helpOption('-h, --help', 'read more information sssss')
  // program.option('-d, --debug <name>', 'choose debug name')
  // program.option('-t, --thread <number>', 'set thread number', '10')
  // program.option('--no-block', 'set is block')
  // console.log(program.parse(['', '', '-h']).opts())
  // console.log(program.parse(['-dt']).opts())
}
